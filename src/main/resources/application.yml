server:
  port : 80
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/GetterzDB?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: kenem-1257-!
  jpa:
    show-sql: true
    hibernate:
      format_sql: true
      ddl-auto: update
credential:
  cryptor:
    master_key: j(^MfTFYeS9ArcNE#)2&&*uU:reX\(&Q
    initial_vector: -@y(c6`V)vqAJc2%
  admin:
    admin_create_key: dare_not_touch_the_sun